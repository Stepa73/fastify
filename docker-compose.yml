version: "3.7"
services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
            target: base
        volumes:
            - './src:/home/node/app/src'
            - './nodemon.json:/home/node/app/nodemon.json'
        expose:
            - '8080'
        ports:
            - '8080:8080'
        command: npm run dev
